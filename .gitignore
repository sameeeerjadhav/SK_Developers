# Local / secrets
config/database.php
config/installed.lock
config/.schema_version
.env
composer.phar
_push_clean.ps1
commit-msg-clean.txt

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# vendor/ is committed so Hostinger git deploy works without running composer
